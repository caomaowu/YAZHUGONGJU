- [x] 左侧侧边栏底部存在常驻“AI 助手”入口，视觉与主题一致
- [x] AI 助手页面为沉浸式双栏布局（会话列表 + 对话区），留白与卡片质感符合 Lavender Pastel
- [x] 配置可保存并持久化到 `server/data/ai_config.json`，且 API Key 不明文回显
- [x] 会话与消息可持久化到 `server/data/ai_chats.json`，支持新建与删除会话
- [x] 发送消息后可流式显示增量内容，体验稳定（无明显卡顿/乱序）
- [x] “停止”按钮可中断生成并保存已生成片段
- [x] AI 消息支持 Markdown（含表格/代码块）与 LaTeX 公式渲染
- [x] 代码块支持语法高亮与一键复制
- [x] Token 统计可见：至少包含本次回复 token 与会话累计 token
- [x] AI 相关后端接口启用鉴权：未登录访问返回 401/403，前端有明确提示
- [x] `npm run check` 与 `npm test` 通过（若新增依赖/代码需补充测试）
- [x] 若存在 E2E，至少覆盖“进入 AI 助手 → 新建会话 → 发送消息 → 收到流式内容”
